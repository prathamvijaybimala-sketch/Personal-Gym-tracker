{
  "name": "gym-pro",
  "version": "1.0.0",
  "description": "GYM PRO - Personal Gym Tracker",
  "scripts": {
    "build": "cp index.html www/index.html && echo 'Web assets ready'",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "@capacitor/android": "^6.1.2"
  }
}
